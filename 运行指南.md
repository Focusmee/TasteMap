# 运行指南（Windows 本机 + MySQL + pnpm）

## 1. 准备环境
- Node.js 18+（建议 18/20）
- pnpm（`npm i -g pnpm`）
- MySQL 5.7+ / 8.0+

> 前端默认访问后端：`http://127.0.0.1:3000`。

## 2. 导入数据库
1) 新建数据库（名字与后端配置一致）：

```sql
CREATE DATABASE IF NOT EXISTS `smart-softwear` DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_0900_ai_ci;
```

2) 导入脚本（脚本内会建表 + 插入 7 天演示数据）：

```bash
mysql -uroot -p smart-softwear < sql_final.sql
```

### 演示账号
- 手机号：`13800000000`
- 密码：`123456`

> 说明：演示账号密码为明文（便于你直接看图表趋势）。后端已做兼容处理：如果数据库里密码不是 bcrypt 格式，会走明文对比。正式环境请用“注册”生成 bcrypt 密码。

## 3. 启动后端（Koa）
在 `backend/backend` 目录：

```bash
pnpm install
pnpm start
# 或 pnpm dev
```

默认端口：`http://localhost:3000`

如果你本机 MySQL 账号密码不同：
- 方式 A：改环境变量

```bash
set DB_HOST=localhost
set DB_PORT=3306
set DB_USER=root
set DB_PASSWORD=你的密码
set DB_NAME=smart-softwear
pnpm start
```

- 方式 B：改 `backend/backend/src/config/database.js`

## 4. 启动前端（Vite + Vue3）
在 `frontend/frontend` 目录：

```bash
pnpm install
pnpm dev
```

访问：`http://localhost:5173`

### 修改后端地址（可选）
前端通过 `src/utils/ipconfig.js` 读取 Vite 环境变量：
- `VITE_SERVER_IP`（默认 127.0.0.1）
- `VITE_SERVER_PORT`（默认 3000）

你可以在 `frontend/frontend` 下创建 `.env`：

```env
VITE_SERVER_IP=127.0.0.1
VITE_SERVER_PORT=3000
VITE_USE_HTTPS=false
```

然后重启 `pnpm dev`。

## 5. 功能入口
- 识别：上传图片 -> 识别记录 -> 可修正名称、加入饮食记录
- 数据看板：7 天趋势（识别 / 出行 / 热量 / 宏量营养）
- 饮食推荐：结合画像与过敏原过滤
- 饮食日历：查看每日摄入、删除记录、查看 7 天趋势
- 知识库：搜索/筛选/对比菜品营养
- 健康咨询：离线规则助手（可替换为模型/LLM）

## 6. 常见问题
### 1) PowerShell 下 curl 报 “Not Found”
`curl http://localhost:3000` 返回 404 属于正常：后端只提供 `/api/*` 接口。
可用以下接口验证：

```bash
curl http://localhost:3000/api/dashboard/summary
```

### 2) 看板没数据
请确认：
1) SQL 是否成功导入（无报错）
2) 使用演示账号登录（user_id=1）或你自己注册的账号是否有数据

